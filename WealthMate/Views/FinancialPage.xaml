<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="WealthMate.Views.FinancialPage"
            xmlns:models="clr-namespace:WealthMate.Models;assembly=WealthMate"
            xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms">

    <NavigationPage.TitleView>
        <AbsoluteLayout x:Name="NavBarLayout">
            <Image x:Name="NavBarLogo"
                   Source="logo.png"
                   HeightRequest="64"
                   WidthRequest="64" />
            <Label x:Name="NavBarTitle"
                   Text="{Binding TotalValue, StringFormat='{0:C}'}"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   FontAttributes="Bold"
                   TextColor="White"
                   FontSize="20"/>
        </AbsoluteLayout>
    </NavigationPage.TitleView>

    <TabbedPage.Children>
        <ContentPage Title="Watchlist">
            <ContentPage.BindingContext>
                <models:Stock/>
            </ContentPage.BindingContext>

            <syncfusion:SfListView x:Name="WatchlistView" AutoFitMode="None" ItemSize="44">
                <syncfusion:SfListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid Margin="12, 0, 12, 0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="{Binding Symbol}"/>
                                <Label Grid.Column="1" Text="{Binding CompanyName}" VerticalOptions="StartAndExpand" FontSize="20" LineBreakMode="TailTruncation" Margin="4, 2, 0, 0" />
                                <Label Grid.Column="1" Text="{Binding Symbol}" VerticalOptions="EndAndExpand" TextColor="DarkGray" FontSize="12" Margin="4, 0, 0, 2"/>
                                <Label Grid.Column="2" Text="{Binding CurrentPrice, StringFormat='{0:C}'}" VerticalOptions="Center" FontSize="16" HorizontalOptions="EndAndExpand" TextColor="ForestGreen" LineBreakMode="NoWrap"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </syncfusion:SfListView.ItemTemplate>
            </syncfusion:SfListView>
        </ContentPage>

        <ContentPage Title="Stocks">
            <StackLayout>
                <BoxView Color="DarkRed" VerticalOptions="FillAndExpand"/>
            </StackLayout>
        </ContentPage>

        <ContentPage Title="Term Deposits">
            <StackLayout>
                <BoxView Color="DarkBlue" VerticalOptions="FillAndExpand"/>
            </StackLayout>
        </ContentPage>

        <ContentPage Title="Savings">
            <StackLayout>
                <BoxView Color="DarkGreen" VerticalOptions="FillAndExpand"/>
            </StackLayout>
        </ContentPage>

        <ContentPage Title="Other">
            <StackLayout>
                <BoxView Color="DarkOrange" VerticalOptions="FillAndExpand"/>
            </StackLayout>
        </ContentPage>
    </TabbedPage.Children>
</TabbedPage>