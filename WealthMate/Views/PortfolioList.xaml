<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WealthMate.Models;assembly=WealthMate"
             xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             mc:Ignorable="d"
             x:Class="WealthMate.Views.PortfolioList"
             Title="List">

  <ContentPage.BindingContext>
    <local:OwnedStock />
  </ContentPage.BindingContext>

  <syncfusion:SfListView x:Name="List"
                         ItemsSource="{Binding OwnedStock}"
                         ItemSize="52">
                         
    <syncfusion:SfListView.ItemTemplate>
      <DataTemplate>
        <Grid Padding="10">
        <Grid.RowDefinitions>
          <RowDefinition Height="0.4*" />
          <RowDefinition Height="0.6*" />
        </Grid.RowDefinitions>
        
        <Label x:Name="testlabel" Text="{Binding purchaseDate}" FontSize="21" FontAttributes="Bold"/>
        <Label Grid.Row="1" Text="{Binding purchasedPrice}" TextColor="Green" FontSize="15"/>
        </Grid>
          </DataTemplate>
    </syncfusion:SfListView.ItemTemplate>                     
    </syncfusion:SfListView>
</ContentPage>