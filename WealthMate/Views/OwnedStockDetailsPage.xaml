<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="WealthMate.Views.OwnedStockDetailsPage">
    
    <NavigationPage.TitleView>
        <AbsoluteLayout x:Name="NavBarLayout">
            <Label Text="{Binding OwnedStock.AssetNameTypePurchasedPrice}" HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center" FontAttributes="Bold" TextColor="White" FontSize="20"/>
        </AbsoluteLayout>
    </NavigationPage.TitleView>
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding OwnedStock.PurchaseDate, StringFormat='Date Purchased: {0:dd MMMM yyyy}'}" FontSize="Medium" Margin="5,0,0,0"/>
            <Label Text="{Binding OwnedStock.SharesPurchased, StringFormat='Shares Purchased: {0:F}'}" FontSize="Medium" Margin="5,0,0,0"/>
            <Label Text="{Binding OwnedStock.PurchasedPrice, StringFormat='Purchased Price: {0:F}'}" FontSize="Medium" Margin="5,0,0,0"/>
            <Label Text="{Binding OwnedStock.CurrentPrice, StringFormat='Purchased Value: {0:F}'}" FontSize="Medium" Margin="5,0,0,0"/>
            <Label Text="{Binding OwnedStock.CurrentValue, StringFormat='Current Value: {0:F}'}" FontSize="Medium" Margin="5,0,0,0"/>
            <Label Text="{Binding OwnedStock.DayReturnToString}" FontSize="Medium" Margin="5,0,0,0"/>
            <Label Text="{Binding OwnedStock.TotalReturnToString}" FontSize="Medium" Margin="5,0,0,0"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>